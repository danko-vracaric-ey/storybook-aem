import React from "react";
import BaseDropdown from "./BaseDropdown";

export default {
  title: "Base/BaseDropdown",
  component: BaseDropdown,
};

const Template = (args) => <BaseDropdown {...args} />;

export const DropdownMenu = Template.bind({});
DropdownMenu.args = {};
