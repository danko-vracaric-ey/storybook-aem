import React from "react";
import FormTextInput from "./FormTextInput";

export default {
  title: "Base/Form/FormTextInput",
  component: FormTextInput,
};

const Template = (args) => <FormTextInput {...args} />;

export const Default = Template.bind({});
Default.args = {};
